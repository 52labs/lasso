# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The production build no longer lands in web/ — Vite writes it to
# server/web/dist (gitignored via the root .gitignore), which the Go binary
# embeds via //go:embed all:web/dist. Run `mise run build` to regenerate it
# locally; CI builds it for releases.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
